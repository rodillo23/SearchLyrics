import React from 'react'

export const SelectList = ({title}) => {
  return (
    <div>
      <label for="">{title}</label>
      <select name="" id="">
        <option value="">----</option>
      </select>
    </div>
  )
}
